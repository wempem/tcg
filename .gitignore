# Build artifacts (regenerated by emcmake + make).
wasm_ncnn/build/

# Local symlink to the prebuilt ncnn-wasm — set up per host.
wasm_ncnn/ncnn

# Raw model assets bundled into static/model/cards.data — kept out of git
# so we don't double-ship them. Regenerate from the source classifiers if
# needed (see README).
wasm_ncnn/assets/

# Python tooling
__pycache__/
*.pyc
.venv/

# Editor/OS noise
.DS_Store
*.swp
